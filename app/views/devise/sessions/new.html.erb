  <div class="mdl-grid">
  <div class="mdl-layout-spacer"></div>
  <div class="mdl-cell mdl-cell--4-col">

    <h3>Iniciar Sesion</h3>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="mdl-textfield mdl-js-textfield">
      <label class="mdl-textfield__label" for="email">Correo Electronico</label>
      <%= f.input :email, required: false, autofocus: true, label: false, :input_html => { :class => 'mdl-textfield__input'} %>
    </div>
    <div class="mdl-textfield mdl-js-textfield">
      <label class="mdl-textfield__label" for="email">Contraseña</label>
      <%= f.input :password, required: false, label: false, :input_html => { :class => 'mdl-textfield__input' } %>
    </div>

    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    <%= f.button :submit, "Iniciar sesion", class: 'mdl-button mdl-button--raised mdl-button--accent' %>
  
  <% end %>
  </div>
  <div class="mdl-layout-spacer"></div>
</div>

<br /><br /><br /><br /><br /><br /><br />
  
